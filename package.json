{
  "name": "androidxrglasses",
  "version": "1.0.0",
  "description": "AndroidXRGlasses.com — News & Encyclopedia for Android XR",
  "scripts": {
    "build": "npx tailwindcss -i input.css -o assets/css/tailwind.css --minify",
    "watch": "npx tailwindcss -i input.css -o assets/css/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
